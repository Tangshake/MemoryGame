
<div class="game-board">
    
    @for(int i=0; i < BoardSize * BoardSize; i++)
    {
        int index = i;
        string cl = GameBoard[index].Class;

        <div class="card @cl" @onclick="() => OnCardTap(index)">
            <img src="@GameBoard[index].Show" />
        </div>
    }

</div>
